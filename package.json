{
  "name": "travo-fullstack",
  "version": "1.0.0",
  "scripts": {
    "start": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd backend && npm install && node server.js",
    "client": "cd frontend && npm install && npm start"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}